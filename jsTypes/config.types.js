/**
 * @typedef {object} DevToRunConfig
 * @property {string} listen_dir 监听目录
 * @property {number} task_delay 任务延迟(秒)
 * @property {string} task_cmd 执行的命令`bash`
 * @property {Function} task_done `task_cmd`执行成功之后的回调
 */
